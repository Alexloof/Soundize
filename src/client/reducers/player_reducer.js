const INITIAL_STATE = {
  isPlaying: false,
  playedTime: 0,
  seeking: false,
  showMusicbar: false
}

export default function(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state
  }
}
